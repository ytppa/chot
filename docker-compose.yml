services:
  postgres:
    image: postgres:17-alpine
    container_name: nothing-chat-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-nothing_chat}
      POSTGRES_USER: ${POSTGRES_USER:-nothing_chat}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-nothing_chat}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:

